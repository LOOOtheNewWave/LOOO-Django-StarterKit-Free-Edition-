# 📘 LOOO Django 환경 변수 예시

# -----------------------------------------------------
# 🌐 Django 기본 설정
# -----------------------------------------------------
DEBUG=True
SECRET_KEY=dev-secret
ALLOWED_HOSTS=*

# -----------------------------------------------------
# 🧱 데이터베이스 설정
# -----------------------------------------------------
# DB_ENGINE: sqlite / mysql
DB_ENGINE=sqlite

# MySQL 사용 시 (sqlite일 때 무시)
DB_NAME=looo_db
DB_USER=root
DB_PASSWORD=password
DB_HOST=db
DB_PORT=3306
